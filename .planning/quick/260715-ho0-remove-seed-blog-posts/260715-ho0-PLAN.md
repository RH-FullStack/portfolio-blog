---
phase: quick
plan: 260715-ho0
type: execute
wave: 1
depends_on: []
files_modified:
  - content/posts/why-i-write-code-toward-japan.mdx
  - content/posts/what-aikido-taught-me-about-shipping.mdx
  - content/posts/starting-to-invest-at-thirty.mdx
autonomous: true
requirements: [REMOVE-SEED-POSTS]
must_haves:
  truths:
    - "The three AI-generated seed posts no longer exist in content/posts/"
    - "The blog system, routes, templates, and lib/query layer remain fully intact for future posts"
    - "`npm run build` succeeds with an empty posts collection (Velite regenerates .velite cleanly)"
    - "The blog index (/blog) renders its existing 'No posts yet' empty state instead of crashing"
    - "No source, test, doc, or asset file references the deleted slugs — nothing 404s or breaks the build"
  artifacts:
    - path: "content/posts"
      provides: "Empty (or real-content-only) posts collection — seed posts removed, blog pipeline untouched"
  key_links:
    - from: "src/lib/posts.ts"
      to: "#site/content"
      via: "getAllPosts() reads the regenerated (now-empty) Velite posts array"
      pattern: "getPublishedPosts"
---

<objective>
Delete the three AI-generated seed/placeholder blog posts from `content/posts/` — `why-i-write-code-toward-japan.mdx`, `what-aikido-taught-me-about-shipping.mdx`, and `starting-to-invest-at-thirty.mdx`. These were example content produced during the Phase 2 blog-system build, not Rasmus's real writing. The entire blog system (routes, templates, Velite pipeline, query layer, related-posts logic) stays intact so Rasmus can drop in his own MDX posts later with zero boilerplate.

Purpose: Ship the site with an honest, empty blog rather than fake AI content under Rasmus's name. The blog infrastructure is already built and verified — this is a content-removal task only.
Output: Three files deleted, a green build proving the pipeline handles zero posts, and a confirmed-clean codebase with no dangling references to the removed slugs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

<facts>
Reference audit already performed during planning (recorded here so the executor does not re-derive it — but Task 1 re-confirms via grep):

- The three seed slugs appear in ZERO source files. A full grep across `src/`, `docs/`, `public/`, `images/`, `assets/` (ts/tsx/js/jsx/mdx/json) returned no matches for `why-i-write-code-toward-japan`, `what-aikido-taught-me-about-shipping`, or `starting-to-invest-at-thirty`.
- Every post surface is DYNAMIC — nothing curates by slug:
  - `src/app/(site)/page.tsx` — "Latest Writing" teaser uses `getAllPosts().slice(0, 3)`. With zero posts it renders just the heading + "View all posts" button (no rows). Not broken.
  - `src/app/(site)/blog/page.tsx` — already has an explicit `posts.length === 0` → "No posts yet" empty state (lines 28-32).
  - `src/app/(site)/blog/[slug]/page.tsx` — `generateStaticParams()` maps `getAllPosts()`; empty → no static post pages; `dynamicParams = false` → unknown slugs 404 at the edge.
  - `src/app/(site)/blog/tags/[tag]/page.tsx` — `generateStaticParams()` maps `getAllTags()`; empty → no tag pages.
  - `src/app/sitemap.ts` — post/tag routes derive from `getAllPosts()`/`getAllTags()`; empty → only static routes remain.
- `src/lib/related-posts.test.ts` uses inline `makePost()` fixtures with invented slugs (`current`, `other`, `two-shared`, …). It does NOT import the real posts array, so deleting content cannot affect it.
- No per-post OG images or cover images named after these slugs exist under `public/`, `images/`, or `assets/`.
- Velite is wired via `next.config.mjs` top-level await (per velite.config.ts comment + STATE.md blocker note), so `npm run build` regenerates `.velite`/`#site/content` before Next compiles. The posts schema is filename-derived slug + manual excerpt; nothing enforces a minimum post count.

Conclusion: this is a pure delete + rebuild-to-verify. No cleanup edits to source are expected. If Task 1's grep unexpectedly finds a reference, STOP and surface it — do not silently edit blog infrastructure.
</facts>

DO NOT modify any file under `src/`, the Velite config, routes, templates, or the query/related-posts layer. The only file-system change is deleting three `.mdx` files. Leave `content/posts/` in place as an empty directory (or with real posts only) so the collection glob still resolves.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the three seed post files and confirm no dangling references</name>
  <files>content/posts/why-i-write-code-toward-japan.mdx, content/posts/what-aikido-taught-me-about-shipping.mdx, content/posts/starting-to-invest-at-thirty.mdx</files>
  <action>Delete exactly these three files with `git rm`: `content/posts/why-i-write-code-toward-japan.mdx`, `content/posts/what-aikido-taught-me-about-shipping.mdx`, `content/posts/starting-to-invest-at-thirty.mdx`. Do NOT delete `content/posts/` itself — the directory must remain so Velite's `posts/*.mdx` glob still resolves to an empty collection. Then re-run the reference audit to prove nothing points at the removed slugs: grep the whole tree (excluding node_modules, .next, .git, .velite, .planning) for each of the three bare slug strings. Expect zero matches (the audit in <facts> already confirmed this). If ANY match is found in a source/test/doc/asset file, STOP and report it in the SUMMARY rather than editing blog infrastructure to "fix" it — that would exceed this task's scope and needs a human decision.</action>
  <verify>
    <automated>test ! -e content/posts/why-i-write-code-toward-japan.mdx && test ! -e content/posts/what-aikido-taught-me-about-shipping.mdx && test ! -e content/posts/starting-to-invest-at-thirty.mdx && test -d content/posts && ! grep -rIn -e "why-i-write-code-toward-japan" -e "what-aikido-taught-me-about-shipping" -e "starting-to-invest-at-thirty" . --exclude-dir=node_modules --exclude-dir=.next --exclude-dir=.git --exclude-dir=.velite --exclude-dir=.planning && echo AUDIT_CLEAN</automated>
  </verify>
  <done>The three seed .mdx files are gone, `content/posts/` still exists, and a full-tree grep for all three slugs returns no matches (prints AUDIT_CLEAN).</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate content and verify the build, tests, and lint pass with zero posts</name>
  <files>(no files created — verification only; Velite regenerates .velite/ which is gitignored build output)</files>
  <action>Prove the blog pipeline handles an empty posts collection end to end. Run `npm run build` — this triggers Velite (wired through next.config.mjs top-level await) to regenerate the `#site/content` module with an empty `posts` array, then compiles Next. The build MUST succeed: `generateStaticParams` in `blog/[slug]` and `blog/tags/[tag]` returns `[]` (no post/tag pages), the sitemap emits only static routes, and the `/blog` index compiles with its existing "No posts yet" empty-state branch. Then run `npm test` (vitest) — the related-posts tests use inline fixtures and MUST still pass. Then run `npm run lint` — MUST pass clean. Do not edit any source to make these pass; if the build genuinely fails on an empty collection, that is a real infrastructure gap — capture the exact error in the SUMMARY and stop.</action>
  <verify>
    <automated>npm run build && npm test && npm run lint</automated>
  </verify>
  <done>`npm run build`, `npm test`, and `npm run lint` all exit 0 with the seed posts removed, confirming the blog system builds and behaves correctly with zero posts.</done>
</task>

</tasks>

<verification>
- `content/posts/` no longer contains any of the three seed posts; the directory itself still exists.
- Full-tree grep for the three slugs returns nothing (no 404 sources, no stale references).
- `npm run build` succeeds (Velite regenerates an empty posts collection; all dynamic post/tag/sitemap surfaces degrade gracefully).
- `npm test` and `npm run lint` pass.
- No file under `src/`, no route/template, and no Velite config was modified.
</verification>

<success_criteria>
The three AI-generated seed blog posts are deleted, the blog system is fully intact and ready for Rasmus's real posts, the site builds/tests/lints green with an empty blog, and nothing anywhere in the codebase references the removed slugs.
</success_criteria>

<output>
Create `.planning/quick/260715-ho0-remove-seed-blog-posts/260715-ho0-SUMMARY.md` when done.
</output>
