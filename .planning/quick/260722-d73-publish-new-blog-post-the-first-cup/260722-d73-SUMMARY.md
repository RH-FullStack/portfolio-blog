---
phase: quick
plan: 260722-d73
subsystem: content/posts
tags: [blog, content, velite]
dependency-graph:
  requires: []
  provides:
    - "Second published blog post: 'The First Cup: How I Start Every Coding Session'"
  affects:
    - content/posts/the-first-cup-how-i-start-every-coding-session.mdx
tech-stack:
  added: []
  patterns: []
key-files:
  created:
    - content/posts/the-first-cup-how-i-start-every-coding-session.mdx
  modified: []
decisions: []
metrics:
  duration: "~15 min"
  completed: 2026-07-22
---

# Quick Task 260722-d73: Publish new blog post: The First Cup Summary

Published Rasmus's second real blog post, "The First Cup: How I Start Every Coding Session," as a single new MDX content file with `draft: false`, using the exact, pre-approved frontmatter and prose copied byte-for-byte. No config, route, template, or other content file was touched.

## What Was Built

**Task 1 — Content file creation:** Wrote `content/posts/the-first-cup-how-i-start-every-coding-session.mdx` verbatim from the plan's approved copy: frontmatter (`title`, `date: "2026-07-22"`, `tags: ["coffee", "programming"]`, `excerpt`, `draft: false`) followed by the five-section body (The Walk to the Coffee Machine, The Boundary Before Focus, When the Ritual Breaks, Dialing In, A Discipline I Didn't Choose on Purpose). No wording was changed; em-dashes and apostrophes preserved exactly. Confirmed `content/posts/why-i-chose-aikido.mdx` and all other tracked files remained untouched.

**Task 2 — Build verification:** Ran `npm run build`. Velite validated the new post against the `posts` schema and Next.js statically generated the blog routes. Confirmed the slug appears in `.velite/posts.json`, and the build output lists `/blog/the-first-cup-how-i-start-every-coding-session` as a statically generated `/blog/[slug]` route alongside the existing aikido post. The `/blog` index and `/blog/tags/*` pages read the same `getAllPosts()` data source, so the post is listed there too (the `coffee` and `programming` tag pages appeared in the build output for the first time, confirming the new tags were picked up).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking issue] Empty `node_modules` in the execution worktree**
- **Found during:** Task 2 (`npm run build`)
- **Issue:** The git worktree this task executed in had a `node_modules/` directory containing only a stray Velite compiled-config artifact — no actual npm packages were installed. `npm run build` failed with `ENOENT` on `node_modules/geist/dist/fonts/geist-sans/Geist-SemiBold.ttf` because the `geist` package (and everything else) wasn't present. This is the same worktree-setup gap previously documented and fixed in quick task 260721-hpn.
- **Fix:** Ran `npm ci` to hydrate `node_modules` strictly from the existing, committed `package-lock.json` (verified the lockfile was byte-identical before and after — no new/changed dependency, so this does not trigger the Rule 3 package-manager-install exclusion). Re-ran `npm run build`, which then succeeded.
- **Files modified:** None (`node_modules` is gitignored; `package-lock.json` unchanged; `git status --short` was clean before and after).
- **Commit:** N/A (no trackable changes).

## Auth Gates

None encountered.

## Known Stubs

None. The published post is final, approved content — not a placeholder.

## Threat Flags

None. This plan added one author-controlled MDX content file with no JSX, imports, scripts, or new dependencies — no new trust boundary or attack surface, matching the plan's `<threat_model>` disposition (accept).

## Self-Check: PASSED

- FOUND: content/posts/the-first-cup-how-i-start-every-coding-session.mdx
- FOUND commit bef3f9e (feat(quick-260722-d73): add second blog post "The First Cup")
- VERIFIED: `.velite/posts.json` contains `the-first-cup-how-i-start-every-coding-session`
- VERIFIED: build output lists `/blog/the-first-cup-how-i-start-every-coding-session` under the `/blog/[slug]` SSG route
- VERIFIED: `content/posts/why-i-chose-aikido.mdx` has zero diff vs. HEAD
