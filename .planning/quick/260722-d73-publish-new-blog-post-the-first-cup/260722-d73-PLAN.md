---
phase: quick-260722-d73
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [content/posts/the-first-cup-how-i-start-every-coding-session.mdx]
autonomous: true
requirements: [BLOG-01, BLOG-02]
must_haves:
  truths:
    - "content/posts/the-first-cup-how-i-start-every-coding-session.mdx exists with the approved frontmatter and body VERBATIM"
    - "draft: false — the post is published (live now), not a draft"
    - "npm run build succeeds — Velite validates the post against the posts schema with no errors"
    - "The post renders at /blog/the-first-cup-how-i-start-every-coding-session (generateStaticParams resolves the new slug)"
    - "The /blog index lists the new post alongside the existing aikido post"
    - "content/posts/why-i-chose-aikido.mdx and all other existing files are unmodified"
  artifacts:
    - path: "content/posts/the-first-cup-how-i-start-every-coding-session.mdx"
      provides: "The 'The First Cup' blog post, published live"
      contains: "The First Cup: How I Start Every Coding Session"
  key_links:
    - from: "velite.config.ts (posts collection)"
      to: "content/posts/the-first-cup-how-i-start-every-coding-session.mdx"
      via: "posts/*.mdx glob pattern → .velite/posts.json"
      pattern: "the-first-cup-how-i-start-every-coding-session"
    - from: "src/lib/posts.ts getAllPosts()"
      to: "#site/content posts array"
      via: "consumed by /blog index and /blog/[slug] detail route"
      pattern: "getAllPosts"
---

<objective>
Publish Rasmus's new blog post at `content/posts/the-first-cup-how-i-start-every-coding-session.mdx` using the exact, already-approved frontmatter and body content, with `draft: false` so it goes live immediately. This is a single content-file addition — no config, route, template, or other content changes.

Purpose: Keep publishing the journey. This is the second real post; it must appear on `/blog` next to the aikido post and render at its own detail route. The copy is final and approved — it is copied byte-for-byte, never rewritten.
Output: One new `.mdx` file that Velite validates and Next.js statically generates at build time.
</objective>

<execution_context>
@$HOME/.claude-account-b/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-account-b/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Content pipeline (DO NOT MODIFY — read only, to match the schema exactly)
@velite.config.ts
@src/lib/posts.ts

# Existing post = the exact pattern to mirror (DO NOT MODIFY)
@content/posts/why-i-chose-aikido.mdx
</context>

<schema_reference>
The `posts` collection schema in `velite.config.ts` (authoritative — match it exactly):

- `title` — string, max 99 chars. (The approved title is 47 chars — passes.)
- `date` — `s.isodate()`, i.e. an ISO date string like `"2026-07-22"`. Passes.
- `tags` — array of strings.
- `excerpt` — string (MANUAL field, D-10 — must be present).
- `draft` — boolean, defaults to false; the approved frontmatter sets it explicitly to `false` (published, live now).
- `metadata` / `code` — computed by Velite from the file itself; NOT written in frontmatter.
- `slug` — derived from the filename (`the-first-cup-how-i-start-every-coding-session`), NOT a frontmatter field. Do NOT add a `slug` key.

The MDX body follows the closing `---` of the frontmatter.
</schema_reference>

<verbatim_file_content>
This is FINAL, APPROVED copy. Write it byte-for-byte EXACTLY as below. Do NOT rewrite, paraphrase, embellish, re-tag, correct "mistakes", or alter the prose in any way. Preserve the em-dash characters (—) and every apostrophe (it's, I'm, isn't, kettle's, that's, wear, etc.) as-is.

```
---
title: "The First Cup: How I Start Every Coding Session"
date: "2026-07-22"
tags: ["coffee", "programming"]
excerpt: "Why I make coffee before I open my laptop, and what that small ritual has taught me about focus, debugging, and getting through the workday."
draft: false
---

## The Walk to the Coffee Machine

I start making coffee before I open my laptop. Every time. It sounds like a small thing, but sitting down with a cup in front of me is what keeps me in line — it's the difference between staring at a blinking cursor and actually settling into work. I can't focus without it. The ritual of grinding, brewing, pouring gets my head into the right space before I've written a single line of code.

The part that actually pays off, though, isn't the drinking — it's the walking. When I get stuck on a bug or can't figure out how a feature should work, I get up and go refill my cup. Walking away from the screen, doing something completely unrelated to the problem, is usually what cracks it open. It's the same thing people mean when they talk about "shower thoughts" — the solution shows up right when you've stopped actively looking for it.

## The Boundary Before Focus

I won't open my laptop before the kettle's on. That's not superstition, it's a boundary. Sitting down with coffee in front of me is what puts me into focus mode, and having a clear ritual to trigger that switch matters more than I expected it to.

I've learned that focus sessions need a length that isn't too short and isn't too long — go too long without a break and it wears me down over time, not just that day. Coffee gives me a natural way to step in and out of focus mode without losing the thread completely. A cup, a refill, a short walk — each one is a checkpoint, not a distraction.

## When the Ritual Breaks

I can feel it immediately if I don't get that first cup before starting my day, whether I'm at the office or working from home. And it's not really about caffeine — I drink decaf just as often. It's that skipping the ritual puts my head in a state it isn't used to for doing this kind of work. The mental switch never gets flipped, and the whole session feels off before it even starts.

That's the part that surprised me most once I paid attention to it: how much of "being ready to work" isn't something I just have. It's something I build, every morning, the same way.

## Dialing In

The reason sitting down with that first cup works as well as it does is routine, plain and simple. I've been doing this long enough that it's become the natural way I start a coding session — not a hack I'm trying, just how it's done now. Without it, I genuinely struggle to focus.

It's a small, low-stakes loop, not unlike debugging: the same input, the same conditions, checked against the same expected result, day after day, until it's not something I think about anymore. It just works, because I've run it enough times to trust it.

## A Discipline I Didn't Choose on Purpose

I don't think coffee should run anyone's life, mine included. This is just the routine I happen to be built around. Other people use a glass of cold water, a smoke break, a walk around the block — the specific ritual doesn't matter much. What matters is having one.

Routines are what keep us in line. They're what get us through the workday, and, if I'm honest, they show up everywhere else too — the same patience that gets me through a hard debugging session is the same patience that gets me through a difficult class on the mat. I didn't set out to build my life around routine. It just turned out that's what was working the whole time.
```
</verbatim_file_content>

<tasks>

<task type="auto">
  <name>Task 1: Create the approved blog post file verbatim</name>
  <files>content/posts/the-first-cup-how-i-start-every-coding-session.mdx</files>
  <action>Create `content/posts/the-first-cup-how-i-start-every-coding-session.mdx` using the Write tool with the content in the `<verbatim_file_content>` section above, byte-for-byte. Place the frontmatter between the two `---` fences, then a blank line, then the MDX body with its five `##` sections. Copy the prose exactly — do NOT reword, embellish, "improve", correct, or re-tag anything; preserve every em-dash and apostrophe. Do NOT add a `slug` field (it is filename-derived per velite.config.ts). Do NOT add `metadata` or `code` (Velite computes those). Do NOT touch `velite.config.ts`, any route, any template, `content/posts/why-i-chose-aikido.mdx`, or any other existing file. This new file is the ONLY file the plan changes.</action>
  <verify>
    <automated>test -f content/posts/the-first-cup-how-i-start-every-coding-session.mdx && grep -q 'The First Cup: How I Start Every Coding Session' content/posts/the-first-cup-how-i-start-every-coding-session.mdx && grep -q 'draft: false' content/posts/the-first-cup-how-i-start-every-coding-session.mdx && grep -q 'tags: \["coffee", "programming"\]' content/posts/the-first-cup-how-i-start-every-coding-session.mdx && git diff --quiet -- content/posts/why-i-chose-aikido.mdx && echo OK</automated>
  </verify>
  <done>The file exists at `content/posts/the-first-cup-how-i-start-every-coding-session.mdx` with the approved frontmatter (title, date 2026-07-22, tags [coffee, programming], excerpt, draft: false) and the five-section body (## The Walk to the Coffee Machine, ## The Boundary Before Focus, ## When the Ritual Breaks, ## Dialing In, ## A Discipline I Didn't Choose on Purpose), verbatim and unaltered. `content/posts/why-i-chose-aikido.mdx` is untouched (git diff clean).</done>
</task>

<task type="auto">
  <name>Task 2: Build and verify the post is generated and surfaced end-to-end</name>
  <files>content/posts/the-first-cup-how-i-start-every-coding-session.mdx</files>
  <action>Run `npm run build`. Velite runs first (wired via next.config.mjs top-level await) and validates the post against the posts schema, then Next.js statically generates the blog routes. Confirm the build exits 0 with no Velite schema errors. Then confirm the post reached the generated content layer and the routes that consume it: the slug appears in `.velite/posts.json`, and the static `/blog/[slug]` detail route resolved the new slug (via generateStaticParams over getAllPosts) so the post detail page renders. The `/blog` index reads the same draft-filtered `getAllPosts()`, so a successful build with the slug present means the post is listed there too. If the build fails on a schema error, re-check the frontmatter matches the `<schema_reference>` exactly — do NOT edit velite.config.ts or any route to make it pass; the fix is always in the content file.</action>
  <verify>
    <automated>npm run build 2>&1 | tee /tmp/d73-build.log | tail -5; grep -q 'the-first-cup-how-i-start-every-coding-session' .velite/posts.json && echo VELITE_OK; grep -Eq '/blog/\[slug\]|the-first-cup' /tmp/d73-build.log && echo ROUTE_OK</automated>
  </verify>
  <done>`npm run build` succeeds (exit 0, no Velite schema errors); `.velite/posts.json` contains the `the-first-cup-how-i-start-every-coding-session` slug; the build output shows the blog `[slug]` route was statically generated. The post is therefore both listed on `/blog` and reachable at `/blog/the-first-cup-how-i-start-every-coding-session`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced. The only change is one author-controlled MDX content file. There is no user input, no network surface, no new dependency, and no package-manager install in this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-d73-01 | Tampering | New MDX content file | accept | Content is plain prose/markdown (no JSX, no imports, no scripts). Velite parses it at build time against a fixed Zod schema; malformed content fails the build rather than reaching production. |
| T-d73-02 | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — nothing to audit. |
</threat_model>

<verification>
- `npm run build` exits 0 with no Velite schema errors.
- `.velite/posts.json` contains the `the-first-cup-how-i-start-every-coding-session` slug.
- Build output lists the statically generated `/blog/[slug]` route.
- `git status` shows exactly one new untracked file (`content/posts/the-first-cup-how-i-start-every-coding-session.mdx`) and no modifications to `content/posts/why-i-chose-aikido.mdx` or any other tracked file.
</verification>

<success_criteria>
- One new file `content/posts/the-first-cup-how-i-start-every-coding-session.mdx` exists with the approved copy verbatim and `draft: false`.
- Velite validates the post; `npm run build` succeeds.
- The post is listed on `/blog` and renders at `/blog/the-first-cup-how-i-start-every-coding-session`.
- No existing file (including the aikido post) is modified.
</success_criteria>

<output>
Create `.planning/quick/260722-d73-publish-new-blog-post-the-first-cup/260722-d73-SUMMARY.md` when done.
</output>
