---
phase: 01-core-site-design-system
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/content/projects.ts
  - src/components/ui/Card.tsx
  - src/components/ui/Tag.tsx
  - src/app/(site)/projects/page.tsx
  - src/app/(site)/page.tsx
autonomous: true
requirements: [PROJ-01, CORE-01, DSGN-01]

must_haves:
  truths:
    - "Visiting `/projects` shows a curated grid of 3-5 projects, each with title, one-line description, tech-stack tags, and links (live demo / code)"
    - "The homepage shows a featured-projects teaser of 2-3 projects that links to the full `/projects` page"
    - "Project demo/code links open safely (rel=noopener noreferrer on external targets)"
    - "The project grid is responsive: single column on mobile, multi-column on wider viewports"
  artifacts:
    - path: "src/content/projects.ts"
      provides: "Typed static project data (single source of truth)"
      exports: ["projects", "Project"]
    - path: "src/components/ui/Card.tsx"
      provides: "Reusable project card primitive"
      min_lines: 15
    - path: "src/components/ui/Tag.tsx"
      provides: "Tech-stack tag/badge primitive (neutral surface, not accent)"
      min_lines: 5
    - path: "src/app/(site)/projects/page.tsx"
      provides: "Projects index page (Server Component, card grid)"
      contains: "projects"
  key_links:
    - from: "src/app/(site)/projects/page.tsx"
      to: "src/content/projects.ts"
      via: "import projects, map to Card"
      pattern: "projects"
    - from: "src/app/(site)/page.tsx"
      to: "src/content/projects.ts"
      via: "featured teaser reads first 2-3 projects"
      pattern: "projects"
    - from: "src/components/ui/Card.tsx"
      to: "src/components/ui/Tag.tsx"
      via: "card renders tech-stack tags"
      pattern: "Tag"
---

## Phase Goal (user story)

**As a** visitor, **I want to** browse Rasmus's curated project showcase and see a teaser of it on the homepage, **so that** I can quickly gauge what he has built and follow through to live demos or code.

<objective>
Ship the Projects vertical slice: a typed project data layer, reusable `Card` + `Tag` primitives, the `/projects` index page (PROJ-01), and the homepage featured-projects teaser that completes the homepage structure (D-07 — CORE-01).

This delivers: **PROJ-01** (curated 3-5 projects with description, tech-stack tags, demo/code links), **CORE-01** (completes the homepage: Hero from plan 01-01 + featured-projects teaser per D-07), and **DSGN-01** (responsive card grid).

Purpose: The same typed project data feeds both the full index and the homepage teaser — one source, two views.
Output: `/projects` page live + homepage teaser wired.
</objective>

<execution_context>
@$HOME/.claude-account-b/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-account-b/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-core-site-design-system/01-RESEARCH.md
@.planning/phases/01-core-site-design-system/01-PATTERNS.md
@.planning/phases/01-core-site-design-system/01-UI-SPEC.md
@.planning/phases/01-core-site-design-system/01-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols/files created by THIS plan (exclude from source-drift verification):
- Type `Project` + const `projects` (`src/content/projects.ts`)
- Component `Card` (`src/components/ui/Card.tsx`)
- Component `Tag` (`src/components/ui/Tag.tsx`)
- File `src/app/(site)/projects/page.tsx` — default export projects index
- (modifies existing `src/app/(site)/page.tsx` from plan 01-01 to append the featured-projects teaser — D-07)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Typed project data layer + Card and Tag primitives</name>
  <files>src/content/projects.ts, src/components/ui/Card.tsx, src/components/ui/Tag.tsx</files>
  <read_first>
    - .planning/phases/01-core-site-design-system/01-RESEARCH.md (Pattern 3 content/projects.ts excerpt; Anti-Pattern — no Velite/MDX in Phase 1; Security Domain — rel on external links)
    - .planning/phases/01-core-site-design-system/01-PATTERNS.md (content/projects.ts canonical block; components/ui/* sibling rule — copy Button's file shape; typed-static-data single-source rule)
    - .planning/phases/01-core-site-design-system/01-UI-SPEC.md (Color — tags use neutral/secondary surface NOT accent; Typography — Label for tag text; Layout — Card hover: slight scale/shadow micro-interaction, D-03)
    - src/components/ui/Button.tsx (existing UI-primitive sibling analog — copy its typed-props + Tailwind-only shape and external-link rel handling)
    - src/content/... (none exists — you are creating the first content module)
  </read_first>
  <action>
    Create `src/content/projects.ts` per PATTERNS.md canonical block: export `type Project = { slug; title; summary; tags: string[]; links: { demo?; code? }; image: { src; alt } }` and `export const projects: Project[]` with 3-5 curated entries (PROJ-01). Use realistic placeholder content Rasmus can edit; each `image.src` points under `/projects/` and each `image.alt` is descriptive. No parsing library, no MDX, no Velite — plain typed TS imported directly into Server Components (Anti-Pattern: introducing Velite in Phase 1 is forbidden). Create `src/components/ui/Tag.tsx` — a small typed primitive rendering tech-stack tag text at Label typography on the NEUTRAL secondary surface (`bg-secondary`/`dark:bg-secondary-dark`), explicitly NOT the vermillion accent (accent stays reserved for interaction/emphasis per UI-SPEC). Create `src/components/ui/Card.tsx` — a typed primitive taking a `Project` (or its display fields) and rendering title (Heading/Body scale), summary, a row of `Tag`s, and demo/code links; external demo/code links render via the `Button`/link primitive with `target="_blank"` + `rel="noopener noreferrer"`; add a subtle hover micro-interaction (slight scale or shadow — D-03) guarded by the global reduced-motion rule already in globals.css.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -12 && grep -q "export const projects" src/content/projects.ts && grep -q "export type Project\|export interface Project" src/content/projects.ts && grep -q "noopener noreferrer" src/components/ui/Card.tsx && grep -Eq "bg-secondary" src/components/ui/Tag.tsx && ! grep -q "velite\|gray-matter\|next-mdx" src/content/projects.ts && echo DATA_OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/projects.ts` exports `Project` and `projects`, with `projects.length` between 3 and 5
    - `projects.ts` imports NO content-parsing library (no `velite`, `gray-matter`, `next-mdx-*`)
    - `Tag.tsx` renders on `bg-secondary`/`dark:bg-secondary-dark` and does NOT use `bg-vermillion` (accent reserved)
    - `Card.tsx` renders external demo/code links with `rel="noopener noreferrer"` and renders `Tag`s for `tags`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Typed project data plus Card and Tag primitives exist and compile; external links are tabnabbing-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Projects index page + homepage featured-projects teaser</name>
  <files>src/app/(site)/projects/page.tsx, src/app/(site)/page.tsx</files>
  <read_first>
    - .planning/phases/01-core-site-design-system/01-UI-SPEC.md (Layout — max-w-6xl page container; Typography — Heading for page title; responsive grid; Spacing — 2xl/3xl section breaks on homepage per D-04)
    - .planning/phases/01-core-site-design-system/01-CONTEXT.md (D-07 homepage = Hero + featured-projects teaser linking to full Projects page; do NOT stub a latest-post slot)
    - .planning/phases/01-core-site-design-system/01-PATTERNS.md (page.tsx Server-Component shape; sibling analog is the existing home page)
    - src/app/(site)/page.tsx (existing hero from plan 01-01 — you are APPENDING the featured teaser, keep the hero intact)
    - src/content/projects.ts (data source — from Task 1)
    - src/components/ui/Card.tsx (from Task 1), src/components/ui/Container.tsx (from plan 01-01)
  </read_first>
  <action>
    Create `src/app/(site)/projects/page.tsx` as a Server Component (no `"use client"`) wrapped in `Container`: a Heading-scale page title ("Projects" or similar), then a responsive grid (single column on mobile, `sm`/`md`+ multi-column per DSGN-01) mapping `projects` to `Card`. Modify `src/app/(site)/page.tsx` to APPEND a featured-projects teaser section BELOW the existing hero (do not touch the hero): a section heading, a grid of the first 2-3 `projects` rendered as `Card`, and a link to `/projects` ("View all projects" or similar). Use generous `2xl`/`3xl` (48-64px) vertical spacing between hero and teaser to honor D-04's whitespace philosophy. Do NOT add a "latest writing"/blog teaser — that is explicitly deferred to Phase 2 (CONTEXT Deferred Ideas).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -12 && grep -q "projects" src/app/\(site\)/projects/page.tsx && grep -q "Card" src/app/\(site\)/projects/page.tsx && grep -q "/projects" src/app/\(site\)/page.tsx && ! grep -qi "latest post\|latest writing\|blog teaser" src/app/\(site\)/page.tsx && echo PAGES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(site)/projects/page.tsx` is a Server Component (no `"use client"`), imports `projects`, and renders them as a responsive `Card` grid inside `Container`
    - `src/app/(site)/page.tsx` retains the hero AND adds a featured-projects teaser of 2-3 cards linking to `/projects`
    - `src/app/(site)/page.tsx` contains NO "latest post"/blog teaser (deferred to Phase 2)
    - `npm run build` prerenders both `/` and `/projects`
  </acceptance_criteria>
  <done>`/projects` shows the curated grid and the homepage teaser links into it; both routes prerender.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| site → external project targets | Each project's demo/code link opens an external site in a new tab |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Project demo/code external links (reverse tabnabbing) | mitigate | Render all `target="_blank"` project links through the `Card`/`Button` primitive that emits `rel="noopener noreferrer"`. |
| T-03-02 | Spoofing/Injection | `content/projects.ts` link URLs | accept | All URLs are developer-authored, hardcoded config values (not user/query-string input) → no open-redirect surface (01-RESEARCH.md Security Domain). |
| T-03-03 | Injection | Card content rendering | accept | Project fields are typed TS strings rendered as text/JSX; no `dangerouslySetInnerHTML`, no MDX in Phase 1. |
</threat_model>

<verification>
- `npm run build` prerenders `/projects` and `/`; `npm run lint` passes.
- `/projects` shows 3-5 cards each with title, summary, tags, and demo/code links; homepage teaser links to `/projects`. Visual/responsive proof finalized in the phase checkpoint (plan 01-05).
</verification>

<success_criteria>
- Curated 3-5 project showcase with description, tags, and demo/code links — PROJ-01.
- Homepage completed with hero + featured-projects teaser — CORE-01 (D-07).
- Responsive card grid — DSGN-01.
</success_criteria>

<output>
Create `.planning/phases/01-core-site-design-system/01-03-SUMMARY.md` when done.
</output>
