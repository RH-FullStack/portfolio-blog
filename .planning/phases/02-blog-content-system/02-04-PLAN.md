---
phase: 02-blog-content-system
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - content/posts/starting-to-invest-at-thirty.mdx
  - content/posts/what-aikido-taught-me-about-shipping.mdx
  - content/posts/why-i-write-code-toward-japan.mdx
autonomous: false
requirements: [BLOG-06]

must_haves:
  truths:
    - "2-3 real MDX launch posts exist, each validating against the Velite schema (D-09) so `npx velite` passes"
    - "Posts cover at least 2 of the 4 content pillars, and at least two posts share a tag (so related-posts + tag archives have genuine matches — D-14)"
    - "Prose is grounded in PROJECT.md biographical specifics (investing at 30 toward 1M DKK; training under Shoji Nishio; first Japan trip = Aikido seminar with 8 partners), not placeholder filler"
    - "At least one post contains a fenced code block (exercises BLOG-03 syntax highlighting on real content)"
    - "Rasmus reviewed and approved each post before it became non-draft (draft:false)"
  artifacts:
    - path: "content/posts/starting-to-invest-at-thirty.mdx"
      provides: "Investing/financial-freedom pillar launch post"
      contains: "tags:"
    - path: "content/posts/what-aikido-taught-me-about-shipping.mdx"
      provides: "Aikido & Japan + software cross-pillar launch post"
      contains: "tags:"
    - path: "content/posts/why-i-write-code-toward-japan.mdx"
      provides: "Software/entrepreneurship + Japan cross-pillar launch post (contains a code block)"
      contains: "```"
  key_links:
    - from: "post frontmatter tags"
      to: "the 4 fixed content pillars (PROJECT.md)"
      via: "flat pillar-aligned tags"
      pattern: "tags:"
    - from: "shared tag across >=2 posts"
      to: "related-posts + /blog/tags/[tag] having real matches"
      via: "overlapping tag values"
      pattern: "tags:"
---

<objective>
Draft 2-3 real launch posts as MDX so tag filtering (BLOG-04) and related posts (BLOG-05) exercise genuine matches at launch, and so BLOG-06's zero-boilerplate publishing workflow is proven on actual content — not placeholder text. Claude writes the prose grounded in PROJECT.md's biography; Rasmus reviews and approves before any post becomes non-draft (D-14).

Purpose: BLOG-06 (proves the write-MDX-and-ship workflow) and the launch-content deliverable D-14. Provides the real content that Wave 3's routes and homepage teaser render and are verified against.
Output: 3 MDX posts in `content/posts/`, tag-aligned to the 4 pillars, approved by Rasmus.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-blog-content-system/02-CONTEXT.md
@.planning/phases/02-blog-content-system/02-01-SUMMARY.md
@src/app/(site)/about/page.tsx

<interfaces>
<!-- Frontmatter schema is fixed by Velite (Plan 02-01) — D-09. Any deviation fails `npx velite`. -->
Required frontmatter (exact keys, from D-09):
```yaml
---
title: "..."           # string, max 99 chars
date: "2026-06-15"     # ISO date (s.isodate())
tags: ["investing"]    # string array, flat, pillar-aligned
excerpt: "..."         # manual one-two sentence hook (D-10)
draft: false           # boolean
---
```
NO `slug` field (filename-derived, BLOG-06). NO `coverImage` field (D-09). Slug = filename without `.mdx`.

The 4 fixed content pillars (PROJECT.md) and suggested flat tags:
- software development & architecture → tag "software"
- building products/SaaS & entrepreneurship → tag "entrepreneurship"
- investing / financial-freedom journey → tag "investing"
- Aikido & Japan → tag "aikido", "japan"
Keep tags FLAT and pillar-aligned — no ad-hoc broad taxonomy (REQUIREMENTS.md Out-of-Scope).

Biographical grounding (PROJECT.md / About page, use as real hooks — not filler):
- Started investing at 30, targeting a 1M DKK portfolio.
- Trained under the late sensei Shoji Nishio.
- First trip to Japan was an international Aikido seminar with 8 training partners.
- Developer building products/SaaS on the side, toward living part of the year in Japan.

Voice reference: src/app/(site)/about/page.tsx — first-person, reflective, the developer/aikidoka/investor threads as one story.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft 3 launch posts as draft-flagged MDX (schema-valid, pillar-tagged, biographically grounded)</name>
  <files>content/posts/starting-to-invest-at-thirty.mdx, content/posts/what-aikido-taught-me-about-shipping.mdx, content/posts/why-i-write-code-toward-japan.mdx</files>
  <read_first>
    - .planning/PROJECT.md (content pillars + biographical specifics)
    - src/app/(site)/about/page.tsx (voice/tone reference)
    - .planning/phases/02-blog-content-system/02-CONTEXT.md (D-09 schema, D-10 excerpt, D-14 content plan, Specific Ideas biographical hooks)
    - .planning/phases/02-blog-content-system/02-01-SUMMARY.md (confirmed frontmatter schema + that `npx velite` fails on malformed frontmatter)
    - velite.config.ts (the schema these posts must satisfy)
  </read_first>
  <action>
    Write three real MDX posts. Set `draft: true` on ALL three for now (they flip to false only after Rasmus approves, Task 2). Cover at least 2 pillars and ensure at least two posts SHARE a tag so related-posts/tag-archives have real matches:

    1. `content/posts/starting-to-invest-at-thirty.mdx` — pillar: investing. Grounded in "started investing at 30, targeting 1M DKK", framed as patience/compounding toward the Japan goal. Tags: `["investing", "japan"]`.
    2. `content/posts/what-aikido-taught-me-about-shipping.mdx` — cross-pillar: aikido + software. Grounded in training under Shoji Nishio and the first-Japan-trip seminar (8 training partners); connect mat discipline to shipping software. Tags: `["aikido", "japan", "software"]`.
    3. `content/posts/why-i-write-code-toward-japan.mdx` — cross-pillar: software/entrepreneurship + Japan. Include at least ONE fenced code block (```ts ... ```) so BLOG-03 syntax highlighting is exercised on real content. Tags: `["software", "entrepreneurship", "japan"]`.

    (Shared tags: "japan" spans all three; "software" spans posts 2 and 3 — guarantees genuine related-posts matches and non-empty tag archives.)

    Each post: complete D-09 frontmatter (title, date — use plausible 2026 dates spread across a few weeks, tags, excerpt as a real one-two-sentence hook per D-10, draft: true), followed by 300-600 words of substantive first-person prose in the About-page voice, with at least a couple of `##` headings (exercises the D-13 heading-anchor plugin). NO `slug` or `coverImage` frontmatter. NO byline in the body (D-12). Do NOT write placeholder/lorem text — use the real biographical hooks above.

    After writing, run `npx velite` to confirm all three validate against the schema (the build FAILS on any malformed frontmatter — that is the BLOG-06 guardrail working).
  </action>
  <verify>
    <automated>npx velite --clean && node -e "const {posts}=require('./.velite'); const slugs=posts.map(p=>p.slug); ['starting-to-invest-at-thirty','what-aikido-taught-me-about-shipping','why-i-write-code-toward-japan'].forEach(s=>{if(!slugs.includes(s))throw new Error('missing post '+s)}); const jp=posts.filter(p=>p.tags.includes('japan')); if(jp.length<2)throw new Error('need >=2 posts sharing a tag')" && grep -q '```' content/posts/why-i-write-code-toward-japan.mdx</automated>
  </verify>
  <acceptance_criteria>
    - All three `.mdx` files exist in `content/posts/` and pass `npx velite` (schema-valid frontmatter)
    - Each has complete D-09 frontmatter (title, date, tags, excerpt, draft) and NO `slug`/`coverImage` keys
    - Tags are flat and pillar-aligned; the tag "japan" appears in ≥2 posts (verified in the velite output)
    - `why-i-write-code-toward-japan.mdx` contains at least one fenced code block (```)
    - Each post has ≥2 `##` headings and 300+ words of real (non-placeholder) prose grounded in PROJECT.md specifics
    - All three currently have `draft: true`
  </acceptance_criteria>
  <done>Three schema-valid, pillar-tagged, biographically grounded draft posts exist and build cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Rasmus reviews the drafts and approves flipping them to non-draft (D-14)</name>
  <what-built>
    Three launch posts drafted (all `draft: true`): starting-to-invest-at-thirty (investing), what-aikido-taught-me-about-shipping (aikido/software), why-i-write-code-toward-japan (software/entrepreneurship, includes a code block). Prose is grounded in the real biography from PROJECT.md.
  </what-built>
  <how-to-verify>
    1. Read the three files in `content/posts/*.mdx` — check the prose reads as your own voice and the biographical details (investing at 30 / 1M DKK; Shoji Nishio; first Japan trip = Aikido seminar with 8 partners) are accurate, not misremembered.
    2. Optionally run `npm run dev` and browse the drafts (the pipeline from 02-01 is live; blog routes arrive in Wave 3, so preview raw or via a dev build).
    3. Edit any prose/titles/tags/dates you want to change directly in the files.
    4. When satisfied, tell Claude which posts to publish — Claude will flip `draft: true` → `draft: false` on the approved posts (any you want held back stay `draft: true` and are excluded from all public views per D-11).
  </how-to-verify>
  <resume-signal>Reply "publish all" to approve all three, or list which posts to publish (others stay draft), or describe edits to make first.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author (Rasmus, via git) → published content | Posts are first-party; the draft flag is a content-visibility gate, not an access-control boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Information Disclosure | Unfinished/private post reachable before approval | mitigate | Posts stay `draft: true` until the human-verify checkpoint; the centralized draft filter (Plan 02-02, D-11) excludes drafts from list/tag/related/sitemap so nothing leaks pre-approval. |
| T-02-09 | Tampering | Malformed frontmatter silently breaking a page | mitigate | Velite Zod schema (Plan 02-01) fails `npx velite`/`next build` on bad frontmatter — a broken post cannot ship as a silently-wrong page (ASVS V5). |
</threat_model>

<verification>
- `npx velite --clean` passes with all three posts present.
- The "japan" tag appears in ≥2 posts (real related-posts/tag-archive matches).
- Human checkpoint completed; approved posts flipped to `draft: false`.
</verification>

<success_criteria>
- Real, approved launch content exists covering ≥2 pillars with shared tags and at least one code block.
- BLOG-06 publishing workflow demonstrated (write MDX + validate, no per-post boilerplate).
</success_criteria>

<output>
After completion, create `.planning/phases/02-blog-content-system/02-04-SUMMARY.md`.
Record the final published slugs and their tag sets, and which (if any) posts remain draft, for the route/homepage plans' verification.
</output>
