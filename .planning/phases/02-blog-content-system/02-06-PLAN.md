---
phase: 02-blog-content-system
plan: 06
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - src/app/(site)/page.tsx
autonomous: true
requirements: [BLOG-01]

must_haves:
  truths:
    - "The homepage shows a 'Latest Writing' section with up to 3 compact post rows (title + date), placed BELOW the existing featured-projects teaser (D-15/D-16/D-17)"
    - "The section has a 'View all posts' link to /blog, mirroring the existing 'View all projects' pattern (D-18)"
    - "The teaser uses PostListRow compact rows, NOT the Card component (D-17) — the homepage never stacks two card grids"
  artifacts:
    - path: "src/app/(site)/page.tsx"
      provides: "Latest-writing teaser section (D-15..D-18)"
      contains: "Latest Writing"
  key_links:
    - from: "src/app/(site)/page.tsx"
      to: "getAllPosts().slice(0, 3) → PostListRow compact"
      via: "module-level constant + map"
      pattern: "getAllPosts\\(\\)\\.slice\\(0, 3\\)"
    - from: "teaser CTA"
      to: "/blog"
      via: "Button href='/blog'"
      pattern: "href=\"/blog\""
---

<objective>
Add the homepage "Latest Writing" teaser (deferred from Phase 1 D-07, now resolved as D-15..D-18) below the existing featured-projects teaser, using the compact `PostListRow` — not the Card grid — so Projects keeps sole ownership of the card treatment and the homepage reads professional-portfolio-first, journey-second.

Purpose: D-15..D-18 (homepage latest-post teaser), reusing the BLOG-01 list rendering in a compact form.
Output: modified `src/app/(site)/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-blog-content-system/02-CONTEXT.md
@.planning/phases/02-blog-content-system/02-PATTERNS.md
@.planning/phases/02-blog-content-system/02-02-SUMMARY.md
@.planning/phases/02-blog-content-system/02-03-SUMMARY.md

<interfaces>
<!-- Current homepage (src/app/(site)/page.tsx) — the existing featured-projects teaser to mirror. -->
Existing pattern in the file:
```tsx
import { projects } from '@/content/projects';
const FEATURED_PROJECTS = projects.slice(0, 3);
// ...
<Container className="py-16 sm:py-24">
  <h2 className="text-heading font-semibold">Featured Projects</h2>
  <div className="mt-12 grid grid-cols-1 gap-8 sm:grid-cols-2 lg:grid-cols-3">
    {FEATURED_PROJECTS.map((project) => (<Card key={project.slug} project={project} />))}
  </div>
  <div className="mt-12 flex justify-center sm:justify-start">
    <Button href="/projects" variant="secondary">View all projects</Button>
  </div>
</Container>
```

From Plan 02-02: `getAllPosts(): Post[]` (published, newest-first).
From Plan 02-03: `<PostListRow post={post} compact />` (title + date only).
Copywriting (UI-SPEC): section CTA copy is exactly "View all posts".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the "Latest Writing" teaser section to the homepage (D-15..D-18)</name>
  <files>src/app/(site)/page.tsx</files>
  <read_first>
    - src/app/(site)/page.tsx (current file — the existing featured-projects teaser section to mirror and place the new section below)
    - .planning/phases/02-blog-content-system/02-PATTERNS.md (homepage page.tsx modify block — exact section markup)
    - .planning/phases/02-blog-content-system/02-CONTEXT.md (D-15/D-16/D-17/D-18)
    - .planning/phases/02-blog-content-system/02-02-SUMMARY.md and 02-03-SUMMARY.md (getAllPosts + PostListRow compact contracts)
  </read_first>
  <action>
    Add a new `<Container className="py-16 sm:py-24">` section AFTER (below) the existing Featured Projects section. Add `import { getAllPosts } from '@/lib/posts'` and `import { PostListRow } from '@/components/blog/PostListRow'` alongside existing imports. Add a module-level constant `const LATEST_POSTS = getAllPosts().slice(0, 3);` mirroring the existing `const FEATURED_PROJECTS = projects.slice(0, 3);`. Section markup:
    ```tsx
    <Container className="py-16 sm:py-24">
      <h2 className="text-heading font-semibold">Latest Writing</h2>
      <div className="mt-12">
        {LATEST_POSTS.map((post) => (
          <PostListRow key={post.slug} post={post} compact />
        ))}
      </div>
      <div className="mt-12 flex justify-center sm:justify-start">
        <Button href="/blog" variant="secondary">
          View all posts
        </Button>
      </div>
    </Container>
    ```
    Use compact `PostListRow` (D-17 — NOT the Card component). CTA copy MUST be exactly "View all posts" linking `/blog` (D-18). Do NOT reorder the sections: Featured Projects stays the primary pitch, Latest Writing sits below it (D-16). If `LATEST_POSTS` is empty, the section renders its heading + CTA with no rows (acceptable — launch content in Plan 02-04 provides posts).
  </action>
  <verify>
    <automated>npx velite --clean && npx tsc --noEmit && grep -q "Latest Writing" src/app/\(site\)/page.tsx && grep -q "getAllPosts().slice(0, 3)" src/app/\(site\)/page.tsx && grep -q "PostListRow" src/app/\(site\)/page.tsx && grep -q "compact" src/app/\(site\)/page.tsx && grep -q "View all posts" src/app/\(site\)/page.tsx && grep -q 'href="/blog"' src/app/\(site\)/page.tsx && ! grep -q "Card" <(grep -A12 "Latest Writing" src/app/\(site\)/page.tsx)</automated>
  </verify>
  <acceptance_criteria>
    - Homepage has a "Latest Writing" `<Container>` section placed AFTER the "Featured Projects" section
    - Renders `getAllPosts().slice(0, 3)` as compact `<PostListRow ... compact />` rows (NOT `Card`)
    - Section has a "View all posts" `Button` linking `/blog` (variant secondary), mirroring "View all projects"
    - Featured Projects remains above and unchanged
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Homepage shows a compact latest-writing teaser below featured projects with a "View all posts" CTA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content query layer → homepage render | Homepage reads published posts through `getAllPosts()` (the draft-safe accessor) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Information Disclosure | Draft post surfacing in the homepage teaser | mitigate | The teaser reads `getAllPosts()`, which routes through the centralized `getPublishedPosts()` draft filter (Plan 02-02, D-11) — drafts cannot appear in the teaser. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `npm run build` renders the homepage with the Latest Writing section populated by the real launch posts (Plan 02-04).
</verification>

<success_criteria>
- Homepage latest-writing teaser (D-15..D-18) is live, compact-styled, links to /blog, and never shows drafts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-blog-content-system/02-06-SUMMARY.md`.
</output>
