---
phase: 03-seo-foundation-launch
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.mjs
  - src/app/(site)/page.tsx
  - public/BackgroundOld.jpg
  - public/heroold.jpg
  - assets/originals/BackgroundOld.jpg
  - assets/originals/heroold.jpg
autonomous: true
requirements: [SEO-01, SEO-04]

must_haves:
  truths:
    - "The hero Background.jpg is served re-encoded to a modern format at a controlled quality (not the raw 412KB JPEG)"
    - "The homepage emits its own hand-written meta description"
    - "Only the true LCP element is marked priority"
    - "Backup image assets no longer live under public/ (not publicly served) but remain in git"
  artifacts:
    - path: "next.config.mjs"
      provides: "images.formats (avif+webp) + images.qualities allow-list"
      contains: "images:"
    - path: "src/app/(site)/page.tsx"
      provides: "Homepage metadata description + quality-controlled hero Image"
      contains: "quality="
    - path: "assets/originals/BackgroundOld.jpg"
      provides: "Relocated backup asset (kept in git, not deployed)"
  key_links:
    - from: "src/app/(site)/page.tsx <Image quality={70}>"
      to: "next.config.mjs images.qualities"
      via: "quality value must be a member of the qualities allow-list"
      pattern: "quality=\\{70\\}"
---

<objective>
Deliver the image-performance foundation for the LCP gate (SEO-04) and the homepage's own metadata (SEO-01), plus relocate backup assets out of `public/` (D-14). This plan exclusively owns `src/app/(site)/page.tsx` and `next.config.mjs` (that is why the homepage description lives here rather than in the plan 03-01 metadata sweep — one owner per file for parallel-safe execution).

Key research finding: `next/image` re-encodes EVERY source at request time based on `next.config.mjs` — pre-converting the JPEG does nothing on its own. The real levers are `images.formats` (currently unset → defaults to WebP-only) and `images.qualities` (Next 16 default `[75]` only; unlisted `quality` props are silently coerced). WebP beats AVIF for this specific textured artwork (verified: WebP q70 ≈ 195KB vs AVIF q70 ≈ 294KB) — do NOT assume AVIF wins.

Purpose: SEO-04 (image optimization config + LCP tuning, D-12/D-13) + SEO-01 (homepage description, D-03) + D-14 (asset relocation).
Output: `next.config.mjs` images config; homepage metadata + tuned hero `<Image>`; backup assets moved to `assets/originals/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-seo-foundation-launch/03-CONTEXT.md
@.planning/phases/03-seo-foundation-launch/03-RESEARCH.md
@.planning/phases/03-seo-foundation-launch/03-PATTERNS.md

<interfaces>
Current next.config.mjs (10 lines) — the Velite top-level-await block MUST be preserved; only add the exported `images` key:
```javascript
// ... existing Velite build() block above (KEEP verbatim) ...
/** @type {import('next').NextConfig} */
export default {}
```

Current homepage hero images (src/app/(site)/page.tsx lines 29-49) — BOTH currently have `priority`:
```tsx
<Image src="/Background.jpg" alt="" fill priority sizes="100vw" className="object-cover" />
<Image src="/hero.jpg" alt={`Portrait of ${siteConfig.name}`} width={200} height={200} priority className="..." />
```

Backup assets to relocate (D-14): public/BackgroundOld.jpg (~409KB), public/heroold.jpg (~11KB).
Empirical compression (this session, sharp on the real Background.jpg): WebP q70 ≈195.5KB, q75 ≈208.5KB; AVIF q70 ≈293.7KB. WebP wins for this image.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure next/image optimization in next.config.mjs</name>
  <files>next.config.mjs</files>
  <read_first>
    - next.config.mjs (preserve the Velite build() top-level-await block verbatim)
    - .planning/phases/03-seo-foundation-launch/03-PATTERNS.md (section "next.config.mjs")
    - .planning/phases/03-seo-foundation-launch/03-RESEARCH.md (Pitfall 2 + Pitfall 3)
  </read_first>
  <action>
Add an `images` key to the default export in `next.config.mjs`, keeping the entire existing Velite `build()` block above it exactly as-is. Set both AVIF and WebP (Next.js negotiates per client; WebP will win for the textured hero, but AVIF is enabled for images where it helps), and an explicit `qualities` allow-list that includes the values plan Task 2 and plan 03-06 may use:

```javascript
/** @type {import('next').NextConfig} */
export default {
  images: {
    formats: ['image/avif', 'image/webp'],
    qualities: [55, 65, 70, 75],
  },
};
```
The `qualities` array intentionally includes 55/65 as headroom so plan 03-06 can dial the hero quality DOWN to hit the Lighthouse-mobile 90+ gate without editing this file again (any explicit `<Image quality>` prop MUST be a member of this array or Next.js silently coerces it — Pitfall 2).
  </action>
  <verify>
    <automated>grep -q "formats: \['image/avif', 'image/webp'\]" next.config.mjs && grep -q "qualities:" next.config.mjs && grep -q "await import('velite')" next.config.mjs && node --check next.config.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `next.config.mjs` default export has `images.formats` = `['image/avif', 'image/webp']`
    - `images.qualities` includes at least `70` and a value `<= 65` for downward headroom
    - The Velite `await import('velite')` / `build(...)` block is still present (`grep -q "await import('velite')" next.config.mjs`)
    - `node --check next.config.mjs` exits 0 (valid ESM syntax)
  </acceptance_criteria>
  <done>next.config.mjs enables AVIF+WebP and a qualities allow-list including a value ≤65 for tuning headroom; Velite wiring preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Homepage metadata description + hero LCP image tuning</name>
  <files>src/app/(site)/page.tsx</files>
  <read_first>
    - src/app/(site)/page.tsx (current hero section, lines 25-61; both images currently `priority`)
    - .planning/phases/03-seo-foundation-launch/03-PATTERNS.md (section "Hero image <Image> usage" + Pitfall 5)
    - .planning/phases/03-seo-foundation-launch/03-RESEARCH.md (Pitfall 5 — priority competition; Assumption A1)
  </read_first>
  <action>
Two changes to `src/app/(site)/page.tsx`:

1. Add a homepage metadata export (D-03 — the homepage gets its own hand-written description; it sets NO title so it inherits the root layout `default` "Rasmus Hansen — Software Developer" per D-02). Add `import type { Metadata } from 'next';` at the top:
```typescript
export const metadata: Metadata = {
  description:
    "Software developer, aikidoka, and long-term investor building toward a life split between Denmark and Japan — explore Rasmus Hansen's projects and writing.",
};
```
Do NOT add a `title` key here.

2. Tune the two hero `<Image>` elements (Pitfall 5 — only the true LCP element should be `priority`; the full-bleed `Background.jpg` is the LCP candidate, the 200×200 portrait is not):
   - On the `Background.jpg` `<Image>`: add `quality={70}` (must match a value in next.config.mjs `images.qualities`). Keep `fill priority sizes="100vw"`.
   - On the `hero.jpg` portrait `<Image>`: REMOVE the `priority` prop (it competes with the LCP background's preload — Assumption A1; small image, gets a reasonable srcset regardless). Keep everything else.

Result target for the two images:
```tsx
<Image src="/Background.jpg" alt="" fill priority quality={70} sizes="100vw" className="object-cover" />
...
<Image src="/hero.jpg" alt={`Portrait of ${siteConfig.name}`} width={200} height={200} className="..." />
```
Note: plan 03-06 may later lower `quality={70}` to `65`/`55` if the Lighthouse mobile Performance gate needs it — that value is already in the qualities allow-list.
  </action>
  <verify>
    <automated>grep -q "export const metadata: Metadata" "src/app/(site)/page.tsx" && grep -q "quality={70}" "src/app/(site)/page.tsx" && test "$(grep -c 'priority' "src/app/(site)/page.tsx")" = "1" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(site)/page.tsx` exports `metadata` with a non-empty `description` and NO `title` key (`grep -q "title:" "src/app/(site)/page.tsx"` returns no match inside the metadata object)
    - The `Background.jpg` `<Image>` has `quality={70}` and retains `priority` + `fill` + `sizes="100vw"`
    - Exactly ONE `priority` occurrence remains in the file (`grep -c priority` == 1) — the portrait no longer has it
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Homepage exports a title-less description; the hero background has a controlled quality and is the sole priority image; type-checks pass.</done>
</task>

<task type="auto">
  <name>Task 3: Relocate backup image assets out of public/ (D-14)</name>
  <files>public/BackgroundOld.jpg, public/heroold.jpg, assets/originals/BackgroundOld.jpg, assets/originals/heroold.jpg</files>
  <read_first>
    - .planning/phases/03-seo-foundation-launch/03-CONTEXT.md (D-14 — backups move to a non-deployed repo folder, kept in git)
  </read_first>
  <action>
Move the two backup assets out of `public/` (where Vercel would serve them publicly) into a repo folder that is NOT under `public/` and therefore is not deployed, while keeping them tracked in git (D-14). Use `git mv` so history is preserved:
```bash
mkdir -p assets/originals
git mv public/BackgroundOld.jpg assets/originals/BackgroundOld.jpg
git mv public/heroold.jpg assets/originals/heroold.jpg
```
Confirm nothing in `src/` references these two files (they were unused backups). Do NOT touch `public/Background.jpg` or `public/hero.jpg` (both are live assets). Do NOT add `assets/` to `.gitignore` — these must stay in git, just not in the deployed `public/` tree.
  </action>
  <verify>
    <automated>test ! -e public/BackgroundOld.jpg && test ! -e public/heroold.jpg && test -f assets/originals/BackgroundOld.jpg && test -f assets/originals/heroold.jpg && test -f public/Background.jpg && test -f public/hero.jpg && ! grep -rq "BackgroundOld\|heroold" src/</automated>
  </verify>
  <acceptance_criteria>
    - `public/BackgroundOld.jpg` and `public/heroold.jpg` no longer exist
    - `assets/originals/BackgroundOld.jpg` and `assets/originals/heroold.jpg` exist
    - Live assets `public/Background.jpg` and `public/hero.jpg` are untouched and present
    - No reference to `BackgroundOld` or `heroold` remains anywhere under `src/`
    - `git status --porcelain` shows the two files as renamed/moved (tracked, not deleted+untracked)
  </acceptance_criteria>
  <done>Backup assets live under assets/originals/ (in git, not served); live hero assets untouched; no dangling references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public/ tree → publicly served static assets | Anything under public/ is world-reachable on the vercel.app domain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Information Disclosure | public/ backup assets | mitigate | Relocating `BackgroundOld.jpg`/`heroold.jpg` out of `public/` stops them being served (D-14); they remain in git for provenance but are not part of the deployed surface |
| T-03-11 | Denial of Service (perf) | hero image optimization | mitigate | Bounded `quality`/`formats` via `images.qualities` allow-list prevents arbitrary large re-encodes and keeps the LCP asset small; only the true LCP element is `priority`, avoiding preload contention |
</threat_model>

<verification>
- `node --check next.config.mjs` + `npx tsc --noEmit` pass
- Backup assets relocated; live assets intact; single priority image on the homepage
- Hero `quality` value is a member of the qualities allow-list
</verification>

<success_criteria>
- SEO-04 (config half): next/image is configured for AVIF+WebP with a qualities allow-list, and the hero LCP image has a controlled quality and sole priority — the levers that actually change served bytes.
- SEO-01 (homepage half): the homepage emits its own description and inherits the root default title.
- D-14: backups no longer deployed.
(The measured Lighthouse 90+ outcome is verified in plan 03-06.)
</success_criteria>

<output>
After completion, create `.planning/phases/03-seo-foundation-launch/03-04-SUMMARY.md`
</output>
