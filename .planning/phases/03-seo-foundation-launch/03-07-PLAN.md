---
phase: 03-seo-foundation-launch
plan: 07
type: execute
wave: 3
depends_on: [6]
files_modified: []
autonomous: false
requirements: [DEPL-01]

user_setup:
  - service: github
    why: "Rasmus connects his PERSONAL GitHub account and pushes the repo himself (HARD GATE D-07); Claude must not create remotes or push"
    dashboard_config:
      - task: "Create a GitHub repo under the personal account and push the local main branch to it"
        location: "GitHub (personal account)"
  - service: vercel
    why: "Vercel Git integration auto-deploys on push (D-08); Rasmus links the repo and picks the vercel.app subdomain (D-09) himself"
    env_vars:
      - name: NEXT_PUBLIC_SITE_URL
        source: "Set in Vercel Project Settings → Environment Variables to the final https://<chosen>.vercel.app URL once the subdomain is known (D-09) — this drives metadataBase, canonical URLs, sitemap, robots, and OG URLs"
    dashboard_config:
      - task: "Import the GitHub repo into Vercel (Hobby tier), confirm Node 20+ build, set NEXT_PUBLIC_SITE_URL, deploy"
        location: "Vercel Dashboard → Add New → Project"

must_haves:
  truths:
    - "No REPLACE_ME placeholder remains anywhere in shipped src/ (launch blocker if present)"
    - "A decision is recorded for project screenshots: real images supplied, OR explicit sign-off to ship monogram placeholders"
    - "The repo is verified deploy-ready and a pre-deploy checklist is presented to Rasmus"
    - "Nothing is pushed to any remote and nothing is deployed until Rasmus gives an explicit go signal (D-07)"
  artifacts:
    - path: ".planning/phases/03-seo-foundation-launch/DEPLOY-CHECKLIST.md"
      provides: "The pre-deploy readiness checklist presented at the gate"
  key_links:
    - from: "pre-deploy verification"
      to: "HARD GATE checkpoint (D-07)"
      via: "build-ready state presented, then STOP for explicit go"
      pattern: "REPLACE_ME"
---

<objective>
Bring the repo to a verified deploy-ready state for the vercel.app launch (DEPL-01), then STOP at the HARD GATE (D-07): Rasmus connects his personal GitHub and Vercel himself and gives an explicit go signal before anything is pushed or deployed. This plan does all the automatable pre-flight checks and presents a checklist — it does NOT create git remotes, push, or run `vercel`.

Purpose: DEPL-01 readiness + D-07 hard gate + D-08/D-09/D-11 launch prerequisites.
Output: an automated pre-deploy verification pass, a `DEPLOY-CHECKLIST.md`, and a blocking checkpoint awaiting Rasmus's go signal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-seo-foundation-launch/03-CONTEXT.md
@.planning/phases/03-seo-foundation-launch/03-RESEARCH.md

<interfaces>
REPLACE_ME occurrences (confirmed this session — 7 across 2 files; Rasmus's to edit per D-11):
  src/lib/site-config.ts: github, linkedin, email
  src/content/projects.ts: 4 project `code` URLs
Verification command (RESEARCH Code Examples):
```bash
grep -rn "REPLACE_ME" src/ && echo "LAUNCH BLOCKED" || echo "clean"
```
NEXT_PUBLIC_SITE_URL is the only intended-public env var (metadataBase/canonical/sitemap/robots/OG). No other NEXT_PUBLIC_ vars should exist. Vercel needs Node 20+ (Tailwind v4 Lightning CSS) — default on new Hobby projects.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-deploy verification + write DEPLOY-CHECKLIST.md</name>
  <files>.planning/phases/03-seo-foundation-launch/DEPLOY-CHECKLIST.md</files>
  <read_first>
    - src/lib/site-config.ts (REPLACE_ME social values — D-11)
    - src/content/projects.ts (REPLACE_ME code URLs + whether real /projects/*.png were supplied)
    - .planning/phases/03-seo-foundation-launch/03-CONTEXT.md (D-07 hard gate, D-08/D-09/D-11)
  </read_first>
  <action>
Run every automatable launch check and capture pass/fail, then write the results into `.planning/phases/03-seo-foundation-launch/DEPLOY-CHECKLIST.md`. Do NOT fill in any REPLACE_ME values or invent screenshots — these are Rasmus's (D-11). The checks:

1. REPLACE_ME scan (LAUNCH BLOCKER if any remain — D-11):
   `grep -rn "REPLACE_ME" src/` → must return nothing. If matches remain, list each file:line in the checklist as an OUTSTANDING blocker for Rasmus.
2. Project screenshots decision: check `public/projects/` for real `*.png` files AND whether `src/content/projects.ts` has restored `image` fields. Record one of two states: (a) real screenshots supplied → good; (b) none → note that cards will render the monogram placeholder (plan 03-05) and this needs Rasmus's explicit sign-off to ship as-is.
3. No broken image references: `grep -rn "src: '/projects/" src/content/projects.ts` returns only paths that exist under `public/` (or none). No dead references.
4. Env var hygiene: confirm the ONLY `NEXT_PUBLIC_` var referenced in the codebase is `NEXT_PUBLIC_SITE_URL` (`grep -rn "NEXT_PUBLIC_" src/ next.config.mjs`), and no domain is hardcoded in sitemap/robots/layout.
5. Production build sanity: confirm plan 03-06 recorded a passing Lighthouse gate (reference its SUMMARY) — do not re-run unless 03-06 evidence is missing.
6. No `output: 'export'` in next.config.mjs (would break Image Optimization + OG/sitemap route handlers — CLAUDE.md).

Write `DEPLOY-CHECKLIST.md` with two sections:
  - "Automated checks" — each check above with PASS / BLOCKED status.
  - "Rasmus's manual steps (HARD GATE — D-07)" — the deploy runbook he executes himself: (1) edit the REPLACE_ME values in site-config.ts + projects.ts; (2) supply real /projects/*.png screenshots and restore the `image` fields (or sign off on placeholders); (3) create a GitHub repo under his personal account and push; (4) import into Vercel (Hobby), confirm Node 20+; (5) set `NEXT_PUBLIC_SITE_URL` to the chosen `https://<subdomain>.vercel.app`; (6) deploy; (7) post-deploy: verify OG preview via a social-preview debugger and that `/sitemap.xml` + `/robots.txt` resolve on the live domain (RESEARCH Pitfall 7).
  </action>
  <verify>
    <automated>test -f .planning/phases/03-seo-foundation-launch/DEPLOY-CHECKLIST.md && grep -q "REPLACE_ME" .planning/phases/03-seo-foundation-launch/DEPLOY-CHECKLIST.md && grep -qi "NEXT_PUBLIC_SITE_URL" .planning/phases/03-seo-foundation-launch/DEPLOY-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `DEPLOY-CHECKLIST.md` exists with an "Automated checks" section recording PASS/BLOCKED for all 6 checks
    - The current `grep -rn "REPLACE_ME" src/` result (count + file:line list, or "clean") is recorded verbatim in the checklist
    - The project-screenshots state (real images vs placeholder-with-sign-off-needed) is recorded
    - A "Rasmus's manual steps (HARD GATE)" runbook section is present covering GitHub push, Vercel import, NEXT_PUBLIC_SITE_URL, and post-deploy OG/sitemap verification
    - The checklist explicitly states Claude has NOT pushed or deployed anything
  </acceptance_criteria>
  <done>All automatable launch checks are run and recorded; DEPLOY-CHECKLIST.md captures readiness state, outstanding blockers, and the manual deploy runbook — with no REPLACE_ME values filled and no screenshots invented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: HARD GATE — present deploy-readiness and wait for explicit go signal</name>
  <what-built>
The full site is deploy-ready: metadata/titles/descriptions on every page, favicon set, static OG image, sitemap.xml + robots.txt, image optimization, project-card 400s eliminated, and a verified Lighthouse 90+ gate (plan 03-06). `DEPLOY-CHECKLIST.md` summarizes readiness and outstanding manual items.
  </what-built>
  <how-to-verify>
1. Open `.planning/phases/03-seo-foundation-launch/DEPLOY-CHECKLIST.md` and review the automated-checks results.
2. Resolve any BLOCKED items yourself (D-07/D-11): edit the REPLACE_ME values (GitHub/LinkedIn/email in `src/lib/site-config.ts`; the 4 project `code` URLs in `src/content/projects.ts`); supply real `public/projects/*.png` screenshots and restore each project's `image` field (or explicitly accept the monogram placeholders for launch).
3. When you are ready to go live, you connect your PERSONAL GitHub and Vercel yourself and run the manual deploy runbook in the checklist — set `NEXT_PUBLIC_SITE_URL` to your chosen `https://<subdomain>.vercel.app`, deploy on the Hobby tier, then confirm the OG preview and that `/sitemap.xml` + `/robots.txt` resolve on the live URL.

IMPORTANT (D-07): Claude has NOT created any git remote, pushed anything, or run any `vercel` command, and will not until you explicitly say so.
  </how-to-verify>
  <resume-signal>Reply "deployed" (with the live vercel.app URL) once you've launched, or "approved to ship as-is" / describe any remaining blockers. Nothing further is automated until you respond — this closes DEPL-01 for the phase.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → public GitHub + Vercel deploy | Source and env config cross into a public, internet-reachable surface (Rasmus-operated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Information Disclosure | placeholder secrets / links | mitigate | Blocking REPLACE_ME scan prevents shipping placeholder social/contact URLs; and enforces that only `NEXT_PUBLIC_SITE_URL` carries the `NEXT_PUBLIC_` prefix (no accidental secret exposure to the client bundle) |
| T-03-17 | Elevation of Privilege / unauthorized deploy | git remote / vercel | mitigate | HARD GATE (D-07): Claude never creates remotes, pushes, or runs `vercel`; deployment is a human-triggered, Rasmus-operated action on his own accounts. The checkpoint is `gate="blocking"` and the plan is `autonomous: false` |
| T-03-18 | Spoofing (wrong canonical host) | NEXT_PUBLIC_SITE_URL | mitigate | Checklist requires setting `NEXT_PUBLIC_SITE_URL` to the real chosen subdomain before/at deploy so metadataBase/canonical/sitemap/robots/OG all advertise the correct host (D-09) rather than the localhost fallback |
</threat_model>

<verification>
- DEPLOY-CHECKLIST.md exists with automated results + manual runbook
- The plan performs NO git remote / push / vercel operations
- Checkpoint blocks until Rasmus's explicit go signal (D-07 satisfied)
</verification>

<success_criteria>
- DEPL-01 readiness: the repo is verified deploy-ready and a complete checklist + runbook is presented; the HARD GATE holds (no push/deploy without Rasmus's explicit go). Actual go-live is Rasmus's action on his own GitHub/Vercel, tracked to closure by his resume signal.
</success_criteria>

<output>
After completion, create `.planning/phases/03-seo-foundation-launch/03-07-SUMMARY.md`
</output>
